# Download the TCGA file 
# Change directory to location of TCGA file
cd ~/Desktop/TCGA_data/

# Check that the current directory is the files of all cases from the TCGA data
ls 

# Unzip all count files for each case
for i in `ls .`; do gunzip ${i}/*.gz; done

# Look at the head of all files
head */*.counts

# Look at the tail of all files
tail */*.counts

# 
paste */*.counts | head 


